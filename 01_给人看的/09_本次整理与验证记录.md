# 本次整理与验证记录

日期：2026-09-07。

- 原项目未搬移或删除，共享包为独立快照。
- 精简重复批次与缓存，保留原始数据和最新验收证据。
- 修复共享副本备兑默认查询路径；在项目外工作目录成功查询576模块和一个最新状态。
- 共享副本50项单元/回归测试通过。
- 当前验收批次结构和独立账本核验通过（--skip-replay）；本次未全量重跑576模块。
- 阅读版Markdown中的链接及图片目标存在性检查通过。
- 科创50仍保留外部gj_db依赖，未验证重新运行；使用现成HTML/Excel阅读研究结果。
- 原始运行证据中的本机路径保留作为历史元数据；不把历史绝对路径作为跨机运行入口。
- 不包含个人集中数据库配置或本地密码配置，数据库示例地址已替换为占位文本。
- 压缩包排除验证过程中产生的Python缓存；清单SHA256用于接收后核对文件内容。
